# ImangeButton

Source: `src/Renderer/Component/ImangeButton.tsx`
Story: none

역할:

- 기존 typo import 호환용 wrapper.
- 실제 구현은 `ImageButton.tsx`를 재-export한다.

현재 UI:

- 자체 UI 없음.

UI 변경 지시 포인트:

- 새 UI 변경은 `ImageButton.md`를 수정한다.

유지할 계약:

- 기존 import가 깨지지 않도록 가능하면 유지한다.
